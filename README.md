# Huepot

Timed color-pot game: same price on every color, biggest color splits the rest of the pot. Accounts, multi-chain deposit wallets, and play USDT balance.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
