# Huepot

Timed color-pot game: same price on every color, biggest color splits the rest of the pot. Google sign-in, one live BNB Chain (BEP-20) USDT deposit address, play balance, and rake invite.

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).
